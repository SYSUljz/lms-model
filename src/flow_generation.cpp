#include "model.cpp"
template <typename T>
void FlowGeneration(StateParam<T> &loc_state_param, ConstParam<T> &const_param, StateParam<T> &target_state, int time_interval)
{
  // calculate evaporate
  if (const_param.label == Label::Soil)
  {
    loc_state_param
  }
}
